apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: stg
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 8082   # acceso externo en localhost:8082
  selector:
    app: frontend
