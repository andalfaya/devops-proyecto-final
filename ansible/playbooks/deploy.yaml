- hosts: local
  vars:
    env: "{{ env }}"
  tasks:
    - name: Restart backend deployment
      command: kubectl rollout restart deployment backend -n {{ env }}

    - name: Restart frontend deployment
      command: kubectl rollout restart deployment frontend -n {{ env }}